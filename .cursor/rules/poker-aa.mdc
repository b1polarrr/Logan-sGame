---
description: Poker_AA 项目协作约定与编码规范
alwaysApply: true
---

# Poker_AA 开发约定

完整说明见 [docs/开发约定.md](../../docs/开发约定.md)。新对话可 @ 该文件。

## AI 协作

- 手搓、一步一步、增量修改；不要默认整文件替换
- 不要单独写「添加 import」步骤（IDEA Alt+Enter）
- 与用户用简体中文交流

## 命名

- 除 Netty 的 `ctx` 外，变量/参数用全称：`gameManager`、`playerSession`、`roomId`
- 禁止 `gm`、`ps` 等缩写
- 单例：`SessionManager.getINSTANCE()`、`SnapshotBroadcaster.getINSTANCE()`；`RoomRouter.getInstance()`

## 代码

- 最小改动，匹配现有风格，不主动 git commit

## 测试与发布

- **不在 `test.html` 联调**；功能改动在 `frontend/`（Vue）实现与验证
- 测试流程：本地改完 → **cmd 推 GitHub** → 阿里云主机 `git pull` → 重建部署后测试
- 前端改动需 rebuild Nginx 镜像（`docker compose ... up -d --build`），仅 pull 不够

## Git 操作

- **一律用 cmd（命令行）** 做 git，不用 IDEA 图形界面
- 常用：`git status` → `git add` → `git commit` → `git push origin main`
- 详见 [docs/SOP-GitHub更新.md](../../docs/SOP-GitHub更新.md)

## 项目上下文

- Java 17 + Netty + Protobuf 扑克后端；WebSocket 路径 `/ws` 端口 8888
- 正式前端：`frontend/`（Vue3 + Vite）；`test.html` 仅历史联调页，不再用于测试
