# 复制为 deploy/.env 供 docker compose 参考（compose 内已写死环境变量）
REDIS_URL=redis://redis:6379
# KAFKA_BOOTSTRAP_SERVERS=kafka:9092
# KAFKA_EVENTS_TOPIC=poker.events

# MySQL（账号登录）
MYSQL_ROOT_PASSWORD=poker_root
MYSQL_DATABASE=poker_aa
MYSQL_USER=poker
MYSQL_PASSWORD=poker123
# IDEA 本地连宿主机映射端口时用：
# JDBC_URL=jdbc:mysql://localhost:3306/poker_aa?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# JDBC_USER=poker
# JDBC_PASSWORD=poker123

# 多 Pod 本地模拟（IDEA 或单容器）
# POD_NAME=game-server-1
# PORT=8888
